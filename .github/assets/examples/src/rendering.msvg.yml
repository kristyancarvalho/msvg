type: sequence
title: "Rendering a post with MSVG"
description: "The interaction between an author, the Markdown file, the build, and the reader's browser."
theme: dark

participants:
  author: "Author"
  markdown: "Markdown file"
  build: "Static build"
  browser: "Browser"

messages:
  - author -> markdown: "writes an msvg block"
  - markdown -> build: "build reads content"
  - build -> build: "generates SVG asset"
  - build -> browser: "serves HTML and image"
